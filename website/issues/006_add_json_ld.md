# Add JSON-LD Structured Data

**Status:** Open
**Priority:** Low
**Tags:** seo

## Description
Add JSON-LD structured data for `SoftwareApplication` to `index.html`. This helps search engines understand the product better and can improve rich snippets in search results.

**Action Items:**
- [ ] Generate JSON-LD schema for VarveDB.
- [ ] Add the `<script type="application/ld+json">` tag to the `<head>` of `index.html`.
